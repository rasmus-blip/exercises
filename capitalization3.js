"use strict";

const name = "Rasmus";
const first = name.substring(0, 2);
const second = name.substring(2, 3).toUpperCase();
const third = name.substring(3, 6);
const result = first + second + third;

console.log(result);
